# 项目上下文与术语表

## 产品定位

- 电商视觉工作流操作台：产品 2.0 目标定位。面向电商运营和视觉生产场景，把素材输入、AI 处理、人审判断、分支继续、资产沉淀和导出串成可复用工作流。
- 电商视觉自助台：产品 1.0 定位。强调用户在 Web 页面中自助完成图片生成、优化和素材管理。
- 自助工作台：当前 Web 主生产入口，承载最高频的商品图套模板流程。

## 工作流概念

- 工作流模板：一套可复用的视觉生产流程，例如商品图套模板、历史成图继续优化、局部重绘修瑕疵、批量上新图生产。
- 工作流运行：一次具体执行记录，包含本次输入、步骤、候选结果、反馈、分支和最终输出。
- 工作流节点：工作流中的一个处理单元，可以是输入、人审、AI 处理、条件判断或输出。
- 下一步动作：用户基于某张图继续进行的操作，例如继续优化、局部重绘、下载、入库、移入回收站。
- 运行路径：一张图从输入到生成、反馈、继续分支的完整路径。当前前台能力中对应「分支路径」。

## 图片与资产概念

- 产品图：被放入模板或参与生成的商品主体图片。
- 模板图：承载商品图的版式、背景或营销视觉模板。
- 参考图：用于约束风格、构图或局部修复方向的辅助图片。
- 历史成图：已经生成并保存的结果图片。
- 候选图：同一次生成任务返回的一组备选结果。
- 父图：当前图上一轮所基于的输入图。
- 子分支：基于当前图继续优化或局部重绘后产生的新结果。
- 同批候选：同一次抽卡或生成任务产生的其他候选图。
- 成品图：未来可能引入的概念，表示经过人审确认、可交付或可复用的图片。当前尚未正式实现。

## 人审与反馈

- 人审：用户对 AI 生成结果进行判断、选择、反馈和下一步操作。
- 可用：当前保留的质量反馈标签，表示图片大体可用。
- 最终图：已在前台下线的概念。因为它与「可用」标签在当前场景中高度重复，容易造成混淆。
- 反馈标签：用户对候选图给出的判断，例如可用、产品不对、模板跑偏、文案变了、自定义标签。

## 架构分层

- Web 操作台：负责上传、选择、框选、预览、人审、下载和素材管理。
- 工作流内核：负责定义流程、记录运行状态、串联步骤、维护父子关系和分支路径。
- 扣子 / Agent 编排层：负责自然语言入口、AI 节点封装、自动化调度、条件分支和生产总结。
